jQuery(document).ready(function($) {

    // Add the_title(); as an alt attribute to any images without one. This is for SEO.
    //$('body').find('img:not([alt])').attr('alt', SiteInfo.the_title);

});